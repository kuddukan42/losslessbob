# Curator-contradicted corpus — embedding second pass (TODO-273 item c)

Generated by `tools/tapematch/emb_second_pass.py` (re-runnable; reads
`observations.db` only, decodes no audio, writes nothing back).

Tier A bar: `addon_links.rule_d.t_emb` = **0.75**, both conventions.
Tier B floor: curator-silent control p95 = **0.643**.

## 1. The embedding separates; the contradicted corpus sits on the control

| population | n | emb p25 | med | p75 | p90 |
| --- | ---: | ---: | ---: | ---: | ---: |
| confirmed same-source (positive) | 1630 | 0.468 | 0.910 | 0.972 | 0.996 |
| curator-contradicted | 1715 | 0.130 | 0.206 | 0.346 | 0.507 |
| curator-silent different (control) | 15339 | 0.128 | 0.219 | 0.323 | 0.551 |

The positive population medians ~4x the other two, and the contradicted
corpus is statistically indistinguishable from the negative control at every
quartile. On the only audio signal here with a calibrated bar, the bulk of
the corpus looks like genuinely different recordings — not like same-source
pairs tapematch failed to merge.

## 2. Tier x bucket (all 1,822 contradicted pairs)

| census bucket | A rule_d-qualifying | B elevated | C control-like | no_emb | total |
| --- | ---: | ---: | ---: | ---: | ---: |
| lb_collision | 0 | 0 | 4 | 4 | 8 |
| label_contradiction | 0 | 8 | 296 | 1 | 305 |
| duration_mismatch | 0 | 13 | 296 | 3 | 312 |
| alignment_failure | 0 | 31 | 721 | 24 | 776 |
| segment_patchwork | 0 | 0 | 4 | 1 | 5 |
| unexplained | 0 | 18 | 324 | 18 | 360 |
| **all** | **0** | **70** | **1645** | **51** | **1766** |

## 3. The 377 `unexplained` pairs — the question this pass was opened for

| tier | n | share |
| --- | ---: | ---: |
| A rule_d-qualifying | 0 | 0.0% |
| B elevated | 18 | 5.0% |
| C control-like | 324 | 90.0% |
| no_emb | 18 | 5.0% |

**Verdict: they are not a hidden failure class.** 90.0% sit in the negative-control band, and the census already showed they are
clean on every metadata axis — speed-locked on both sides, duration ratio
within 15%, no disclaimer text, no LB-tag collision. The most economical
reading is curator label noise with no textual marker: the claim is wrong
(or was mis-parsed) and the recordings really are different.

**Stated honestly:** tier C is *absence of evidence*. The embedding recalls only 59% of confirmed
same-source pairs at this floor, so a control-like score does not prove two
recordings differ. What tier C does establish is that no signal tapematch
currently persists puts these pairs anywhere near a merge bar — consistent
with census finding 5 (corr <0.05 for 93.3%, never reaching 0.40). Settling
them either way needs a signal the project does not have today, and the
population is not large or distinctive enough to justify building one.

## 4. Tier A — pairs the SHIPPED bar already merges (BUG-278)

0 contradicted pairs clear `rule_d`'s calibrated both-convention bar
yet are stored `different_family`. This is not a matcher gap — the rule that
would merge them shipped on 2026-07-04. It cannot fire in a live session:
`tapematch/cli.py`'s `_pair_metrics()` never puts `emb_score` /
`emb_score_global` in the dict handed to `verdict.pair_links`, so
`_rule_d_emb_both` reads `None` and abstains on every pair. `emb_live.py`
does populate the columns, but from `_log_to_obs_db()` — after clustering has
already run and the verdict has been written. Filed as **BUG-278**.

Corpus-wide effect of linking rule_d and re-closing each date transitively:

- **0** pairs the curator claims as same-source would flip to `same_family` (rescues).
- **2** pairs the curator is silent on would also flip (unvalidated — see below).
- across **1** dates.

### Validation of the flips (2026-07-27)

rule_d's zero-new-FP proof covers the 2,245-pair frozen sets only, so the
corpus-wide flips were scored separately against the curator's own stance.
Splitting every pair by what the curator actually says — `lb_says_same=1`
(claims same), `=0` (**explicit denial**), `NULL` (silent, no statement) — the
rule discriminates sharply:

| curator stance | rule_d fires | of n | rate |
| --- | ---: | ---: | ---: |
| claims same | 732 | 3,631 | 20.16% |
| silent (no statement) | 721 | 17,293 | 4.17% |
| **explicit denial** | **12** | **3,038** | **0.39%** |

A **52x** gap between claims-same and explicit-denial. The rule is tracking
source identity, not firing indiscriminately. Of the 12 denial hits, 10 already
carry corr >= 0.83 — tapematch's primary signal merges them regardless, so they
are curator label noise (the TODO-201 class), not rule_d errors.

Of the 2 flips: 0 the curator claims as same-source,
1 the curator is silent on, and **exactly 1** contradicts an explicit
denial — 1999-11-09 LB-02737/LB-04289 (emb 0.872/0.937, corr 0.101), where the
LB-4289 notes say "different recording than LB-1401, LB-2064, and LB-2737 based
on different crowd at begin of d1t2". That single pair is the entire measured
false-positive exposure of wiring rule_d live. **The gate passes.**

Two watch items for the fix, neither blocking:

- **1978-03-09** is the only date (>=4 sources) where rule_d links *every* pair,
  collapsing the whole date into one family. Spot-check it before/after.
- 156 of 793 rule_d-firing dates have a median emb >= 0.60 across all their
  pairs. Elevated per-date baselines are the same-show/different-source confound
  BASELINE.md Task 8 documented; the both-convention requirement appears to
  handle it (those dates are not where the denial hits land), but a per-date
  baseline is the natural guard if rule_d is ever loosened below 0.75.

| date | pair | emb | emb_global | corr | census bucket |
| --- | --- | ---: | ---: | ---: | --- |

## 5. Tier B — elevated but not merge-grade

70 pairs score above the control p95 but below the rule_d bar. They are
the natural evaluation set for any future loosening of `t_emb`, and the
natural first audio-review batch if one is ever run. No action proposed here:
the 0.700 step of rule_d's own sweep already cost abs fp 11 vs 9, so the bar
is sitting one step off a known cliff and should not be moved casually.

