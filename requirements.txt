# Core
pybloom-live==4.0.0
flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
beautifulsoup4==4.12.3
lxml==6.1.0
torf==4.3.1
keyring==25.7.0

# File watching
watchdog==4.0.1

# Fast non-cryptographic hashing (file-level integrity / bit-rot scans, TODO-267).
# Was previously present only as a transitive dep of pybloom_live — pinned
# directly now that backend/file_integrity.py imports it.
xxhash==3.7.0

waitress==3.0.0

# Audio processing (Concert Ranker)
numpy==2.4.6
soundfile==0.13.1
scipy==1.17.1

# TapeMatch banter/ASR signal (FABLE_TAPEMATCH_LISTENING_SIGNALS.md §3).
# OPTIONAL + feature-gated: imported lazily by tools/tapematch/tapematch/asr.py
# and only when config.yaml `asr.enabled` is true (it ships false). If the
# package is absent the signal degrades to NULL and the pipeline is unaffected,
# so this line may be dropped from a minimal install. Pulls ctranslate2 +
# onnxruntime + tokenizers; resolves against the pinned numpy above.
faster-whisper==1.2.1

# OPTIONAL, GPU ONLY: CTranslate2 links these at first CUDA use. Without them a
# CUDA decode fails per window rather than at load, which reads as "the tape is
# silent" — tools/bobtalk_locate.py therefore probes them via preload_cuda_libs()
# and falls back to CPU instead of trusting the result. Omit on a CPU-only or
# minimal install; the TODO-303 locate pass then runs ~14x slower but correctly.
# Roughly 1.2 GB installed. pip needs scratch space on a large filesystem —
# TMPDIR=~/.cache/pip-tmp if /tmp is small.
nvidia-cublas-cu12==12.9.1.4
nvidia-cudnn-cu12==9.13.1.26

# Packaging (run separately, not imported)
# pyinstaller==6.8.0
